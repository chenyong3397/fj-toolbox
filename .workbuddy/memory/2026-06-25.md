## 底部信息栏统一风格 + 弱化

- 用户要求：服务状态、访问计数、开发者卡片三个区域背景统一、合理布局、弱化显示
- 将原来分散的三个区块（status-bar + footer/footer-visit + footer-credit/footer-sub）合并为单一 `.meta-bar` 容器
- 统一使用 `#FAFAFA` 浅灰背景、`#B0B0B0` 低对比文字、`22rpx` 小字号

## 职称证查询 - 结果卡片优化

- 移除了结果卡片点击弹出详情弹窗（viewDetail），卡片本身已展示全部信息
- 卡片右上角新增级别标签（绝对定位，渐变背景）：
  - 初级 → 蓝色渐变 `#42A5F5→#1E88E5`
  - 中级 → 橙色渐变 `#FFA726→#EF6C00`
  - 高级(副高) → 红色渐变 `#EF5350→#C62828`
  - 高级(正高) → 紫色渐变 `#AB47BC→#6A1B9A`
- 标签样式：白色文字、24rpx加粗、圆角右上右下，通过 `levelCode` 字段匹配CSS类名

## 职称证查询修复：前端移除验证码输入

- 用户反馈"职称查询时还是一直显示验证码错误"
- 后端 `blueprints/cert_title.py` 已完整重写：集成 ddddocr 自动 OCR 识别图形验证码（最多3次重试），补全所有缺失的查询参数（age303/age306/accountType/age304/agb017）
- 本次完成前端改造：
  - `cert_title.wxml`：移除验证码输入框+图片区域，更新引导步骤为"系统自动处理验证"
  - `cert_title.js`：移除 captcha/captchaImage data、onCaptchaInput()、refreshCaptcha()；doSearch() 不再校验和发送验证码；doClear() 清理相关逻辑
  - 后端：移除已废弃的 `GET /captcha` 调试端点和 Response import
- 查询流程简化为：填写姓名 + 证件号码 → 点击查询（后端自动处理验证码）

## 职称证查询结果 1:1 还原原网页

- 用户要求用测试数据（陈能辉/350122199112175214）1:1还原12333原网页内容
- **根因**：数据源字段名错误（`data.list` 应为 `data.str`），字段映射大量错误
- **后端修复**：
  - 数据源：`data_block.get('list')` → `json.loads(data_block.get('str'))`（12333把数组放在str的JSON字符串里）
  - 字段映射修正：committee `age018`→`age005`, onJobUnit `aab004`→`agb017`, approveDate `age116`→`age007`（并格式化为YYYY-MM-DD）
  - 新增代码映射表：AGE013（级别1=初级/2=中级/3=副高/4=正高）、AGZ066（专业领域）、AAC058（证件类型）
  - 新增字段：`level`(级别中文名)、`levelCode`(原始代码)、`category`(职称类别)、`specialtyDetail`(详细专业)、`certType`(证件类型中文)、`regionCode`(行政区划)
  - `certLevel` 原本错误地用 AGE002（类别"工程技术人才"）映射，现改为用 AGE013 映射正确级别
- **前端修复**：
  - WXML卡片重构：头部显示姓名+证书编号+级别标签，正文增加专业名称、批准日期行
  - JS viewDetail：显示全部11个字段
  - WXSS：新增 `.rch-left`/`.rch-certno`/`.level-tag`/`.level-{1,2,3,4}` 样式，级别标签分色（初级蓝/中级橙/高级红）
- ddddocr需要安装在portable_venv（之前未安装导致OCR不可用）- 三行之间用 `#F0F0F0` 极细分隔线隔开，无图标的简约风格
- 删除了原来的深色渐变背景 + 蓝色发光文字等显眼样式
- 删除了"服务状态"section-title 标签

## 三个功能收纳到「资质」分类卡片

- 用户要求：三个功能收纳到一个卡片内，归类于"资质"
- `app.js`：`globalData.features`（扁平数组）→ `globalData.categories`（分类数组），每个分类含 name/icon/features
- `index.js`：data 从 `features` 改为 `categories`；`goFeature()` 遍历 categories 查找；`tryLoadFromServer()` 服务器数据也包装为 `[{ name:'资质', icon:'📋', features:[...] }]`
- `index.wxml`：外层 `wx:for="{{categories}}"` 渲染分类卡片，内层 `wx:for="{{cat.features}}"` 渲染子功能行
- `index.wxss`：新增 `.category-card`（白色圆角卡片含 header + 子项）、`.cat-header`（分类标题行+功能数标注）、`.sub-item`（子功能行，60rpx 小图标 + 缩小字号）、`.sub-item:not(:last-child)::after` 左缩进分隔线
- 视觉效果：一张白色卡片，顶部「📋 资质 · 3个功能」，下方三行子功能用浅灰线分隔

## 子功能改为横向网格布局

- 用户要求：三个功能横向分布
- `index.wxml`：`sub-item` 列表 → `cat-grid` + `cat-grid-item`（flex 横向排列，每项纵向：图标在上名称在下）
- `index.wxss`：`.cat-grid` display:flex；`.cat-grid-item` flex:1 等宽居中；`.grid-icon` 80rpx 圆角彩色方块；`.grid-name` 22rpx 居中文字

## 新增「证书」板块（3个功能模块骨架）

- 用户要求：开发第二板块"证书"，含注册证、职称证、岗位证
- **后端蓝图**（3个新建）：
  - `blueprints/cert_registration.py` → /api/cert_registration，名称"注册证"，图标📘，色#5C6BC0
  - `blueprints/cert_title.py` → /api/cert_title，名称"职称证"，图标🎓，色#7E57C2
  - `blueprints/cert_position.py` → /api/cert_position，名称"岗位证"，图标🪪，色#26A69A
  - 每个蓝图含 get_info() + /info 端点，/search 待开发
- **前端页面**（3个新建，各含 js/wxml/wxss/json）：
  - `pages/cert_registration/` — 欢迎卡片（3步引导 + "功能开发中"提示）
  - `pages/cert_title/` — 同上，紫色主题
  - `pages/cert_position/` — 同上，青色主题
- **app.js**：categories 数组新增第二项 { name:'证书', icon:'📜', features:[3个] }
- **app.json**：pages 新增3条路径
- **index.js**：tryLoadFromServer() 重写为按 certKeys 分组，资质模块→资质分类，证书模块→证书分类
- Flask 重启后共 8 个模块全部注册成功，/modules 和各 /info 端点验证通过

## 资质模块功能名称简化

- 用户要求：资质异常监控→异常预警，资质审批状态→审批状态，资质人员获取→技术人员
- 修改 `app.js` categories 中三个 feature 的 name 字段
- 修改 `blueprints/qualify_abnormal.py` get_info() name 字段（改为"异常预警"）
- 修改 `blueprints/company_monitor.py` get_info() name 字段（改为"审批状态"）
- 修改 `blueprints/tech_staff.py` get_info() name 字段（改为"技术人员"）
- 注意：前后端都要改，否则 onShow 时服务器 /modules 数据会覆盖本地修改

## 岗位证查询功能移植

- 数据来源：福建省建设人才与科技发展中心 http://220.160.52.118:9084/portalWeb/fwZhcx/toFwZhcxPage?code=zscx
- 外部API：POST http://220.160.52.118:9084/portalWeb/fwZhcx/findCertificate，参数 name + idenNum
- 照片接口：GET /portalWeb/fwZhcx/findPhotoByPersonGuid?personGuid=xxx
- **后端 `blueprints/cert_position.py`**：完整重写，3个端点：
  - `GET /info` — 功能说明
  - `POST /search` — 代理外部查询，返回标准化JSON（code/msg/data/total），data 数组含 userName/sex/certificateType/positionName/certificateNum/starTime/endTime/certificateOrgan/reviewTime/workUnit/certificateStatus/personGuid 等
  - `GET /photo` — 代理照片获取，返回 image/jpeg 二进制流
- **前端 `pages/cert_position/`**：
  - `cert_position.js`：搜索表单(name+idCard) → api.post('/api/cert_position/search') → 结果渲染 + 照片URL拼接
  - `cert_position.wxml`：搜索卡片(姓名+身份证号输入框+查询/清空按钮) → 加载/错误/空状态 → 证书卡片列表(含照片+完整信息)
  - `cert_position.wxss`：青色主题(#26A69A)，搜索卡片+证书卡片样式
- Flask 无全局认证拦截器，photo 端点可直接通过 image src 访问

## 岗位证查询结果添加"查看专业"功能

- 用户要求：证书结果中 careerCode 以 17 开头的检测试验人员证书显示"查看专业"按钮，点击跳转专业详情
- **后端 `cert_position.py`**：
  - search 回传 `careerCode` 字段 + `hasSpecialty` 布尔值（是否以 17 开头）
  - 新增 `POST /api/cert_position/specialty` 端点，代理外部 `POST /baseData/findMsgByPersonGuidByJczzyPr`，参数 certificateGuid
  - 返回标准化 JSON：data 数组含 majorName(大专业)/minorName(小专业)/entryTime(入库时间)
- **前端**：
  - 新建 `pages/cert_specialty/` 子页面（js/wxml/wxss/json），顶部青色渐变证书概览卡片 + 三列表格（大专业/小专业/入库时间）
  - `cert_position.wxml`：在所属地区下方条件渲染 `wx:if="{{item.hasSpecialty}}"` 青色"查看专业"按钮
  - `cert_position.js`：`viewSpecialty()` 传递 certificateGuid + 证书信息(JSON编码)，`wx.navigateTo` 跳转
  - `cert_position.wxss`：`.specialty-btn-box` 顶部浅灰分隔线 + `.btn-specialty` 青色按钮样式
  - `app.json`：pages 注册 `pages/cert_specialty/cert_specialty`

## 注册证查询功能移植（四库一平台从业人员查询）

- 数据来源：全国建筑市场监管公共服务平台（四库一平台）https://jzsc.mohurd.gov.cn
- API路径变换规则：`/dataservice/query/staff/list` → `/APi/webApi/dataservice/query/staff/list`
- **AES解密**：Key=`Dt8j9wGw%6HbxfFn`(16字节), IV=`0123456789ABCDEF`(16字节), CBC, Pkcs7；响应Hex→二进制→AES解密→JSON
- 请求头：`v: 231012`, Referer: `https://jzsc.mohurd.gov.cn/data/person`
- **后端 `blueprints/cert_registration.py`** 完整重写：
  - `jzsc_decrypt(hex_str)` — AES解密工具函数
  - `jzsc_get(url, params)` — 统一GET请求+解密封装
  - `POST /search` — 代理 staff/list，参数 name/idCard/page/pageSize，返回标准化 JSON（list含 ryId/name/idCard/regTypeName/regSealCode/regQymc/regSdate）
  - `POST /detail` — 代理 staffDetailAndTrack，参数 ryId，返回 basicInfo(name/gender/idCard/cardType/status) + certList(regCertNo/regTypeName/regProfName/regSealCode/regQymc/regSdate/regEdate/regStatus/changeNum/trackList)
- **前端 `pages/cert_registration/`** 完整重写：
  - 搜索表单：姓名 + 身份证号 + 查询/清空按钮
  - 结果列表：左蓝边框卡片，显示姓名+注册类别标签+身份证号+注册号+注册单位，支持分页
  - 引导卡片：首次加载时显示3步引导
- **前端 `pages/cert_reg_detail/` 新建**：
  - 人员基本信息卡片：渐变色头像+姓名+状态(正常绿/其他橙)+性别+证件类型+身份证号
  - 注册证书列表：每张证书含类型/状态/编号/专业/注册单位/有效期/变更次数
  - 注册轨迹时间轴：左侧竖线+圆点，按时间倒序
- `app.json` 注册 `pages/cert_reg_detail/cert_reg_detail`
- `app.js` 注册证描述改为"输入姓名或身份证号，查询建设行业注册证信息"
- 安装 pycryptodome 依赖用于 AES 解密
- 所有接口通过 Flask 代理，绕过四库一平台的跨域限制

## 职称证查询 Flask 后端验证与修复

- 启动 Flask 服务，发现端口 5678 有 3 个僵尸 Python 进程导致旧版 cert_title.py 响应 — 已清理
- 安装缺失依赖 numpy + pycryptodome
- 搜索请求修复：添加 `pfaId` 参数、优化请求头（Accept + charset=UTF-8）
- 改进非JSON响应错误处理：区分验证码错误/会话过期/服务繁忙
- **全端点验证通过**：
  - `GET /info` — 返回"查询全国职称评审信息（试运行）"及3个端点说明
  - `GET /captcha` — 返回1625字节 JPEG 验证码图片
  - `GET /cert_types` — 返回6种证件类型（身份证/军官证/护照等）
  - `POST /search` — 正确代理到12333服务器，错误验证码返回"验证码错误，请重新输入"（上游JSON解析成功）
- **查询结果改为2列表格布局**：参照12333原网页样式，紫色左边框卡片 + 5行2列表格。字段映射：姓名/证件类型、证件号码/证书编号、职称系列/职称名称和级别、评审专业名称/发证日期、评审机构/发证机构。奇数行白底，偶数行浅灰背景(.tbl-row:nth-child(even))
- 移除前端证书编号输入框（用户确认选填），搜索表单仅保留姓名+证件类型+证件号码
- **会话过期自动重新登录**：12333返回"因长时间未操作，请刷新重试"时，旧代码只检测'登录'/'login'漏掉了超时提示。新增 `_force_relogin()`（清理login/pubkey/unitInfo全部缓存）和 `_is_session_expired()`（检测"未操作/刷新/长时间/登录/过期/超时"），/search 外层增加 `for retry_round in range(2)` 重登录轮次，非JSON和JSON响应中均检测超时关键字后触发强制重登录。

## 首页标题区改造：动态名言 + 头像融合

- 用户要求：(1) "福建省环宇工程质量检测有限公司"改为每次加载随机的网络流行语/名人名言；(2) 将卡通头像PNG融合到标题背景
- **index.js**：新增25条名言库 QUOTES（14条名人名言 + 11条网络流行语），`getRandomQuote()` 随机选取；`onShow()` 每次切回首页刷新名言

- **免费部署准备**：创建 `requirements.txt`（Flask+ddddocr+pycryptodome等）、`runtime.txt`（Python 3.11.11）、`.gitignore`（排除 miniprogram/portable_venv 等）；`app.py` PORT 改为 `int(os.environ.get('PORT', 5678))` 兼容 Render 环境变量。推荐 Render.com 免费托管方案。
- 新增 `images/header-bg.png`：漫画线稿1.png → PIL 处理为 375px 宽 × 50%透明度 → 铺满 header 背景（`aspectFill` + `opacity:0.5`），文字 z-index:1 保持在顶层
- 图片资源：`miniprogram/images/header-avatar.png`（从剪贴板复制）
- **漫画线稿背景**：用户要求将漫画线稿图以50%透明度作为标题背景并移除白色大圆。PIL 处理图片（375px宽，alpha通道×0.5），保存为 `images/header-bg.png`。WXML 新增 `<image class="header-bg">` 绝对定位在 header 底部，WXSS 移除 `::after` 装饰圆，`.header-text` 加 `z-index:1` 确保文字浮于背景之上。
